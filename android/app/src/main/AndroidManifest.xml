<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Native Google Sign-In redirect only (src/lib/device/oauth.ts,
                 docs/audit/FINDINGS.md LOKL-006). Scheme is the app's own reverse-DNS
                 applicationId — deliberately chosen to avoid colliding with any other
                 installed app's custom scheme, per Android's own convention for this.
                 This is a separate, permanent public interface from
                 capacitor.config.ts's server.androidScheme/iosScheme (the WebView's
                 own origin) — never point this at that scheme or vice versa. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.lokl.app" android:host="oauth-callback" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Geolocation: coarse only. The feed and search are radius-based (1-25 km
         presets, see src/lib/constants.ts radiusOptions) and never need
         street-level accuracy, so we deliberately do not request
         ACCESS_FINE_LOCATION — coarse (~2 km, network/wifi-based) is enough to
         resolve a locality, is easier to justify in the Play Data Safety form,
         and grants at a higher rate. See docs/audit/FINDINGS.md LOKL-003. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <!-- No CAMERA or storage permission is declared here: @capacitor/camera
         launches the system camera/gallery apps via intent (see its own bundled
         <queries> block) rather than accessing hardware directly, and this app
         never sets saveToGallery, so the plugin's storage-permission path is
         never exercised either. -->
</manifest>
