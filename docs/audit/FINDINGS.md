# Lokl — Store-Readiness Remediation: Findings Ledger

Single tracking document for the entire remediation. Every finding has a stable ID referenced by later steps. IDs are never renumbered once assigned — re-adjudication updates a row's `Severity`/description in place rather than issuing a new ID for the same underlying issue. `Status` starts at `OPEN` for everything except items explicitly closed in this step.

This version merges: the Step 0 seed list, the prior full pre-launch audit's complete P0/P1/P2 tables, Step 0 Task 3's gap-closure findings, and Step 0 Task 4's re-adjudications. Deduplicated — nothing appears twice under two IDs.

| ID | Severity | Title | Evidence (file:line) | Store affected | Status | Fixed in step | Verified how |
|---|---|---|---|---|---|---|---|
| LOKL-001 | Fixed | iOS `Info.plist` has no permission usage-description strings, but code calls `navigator.geolocation` and uses file inputs for camera/photo access | `ios/App/App/Info.plist` (added `NSLocationWhenInUseUsageDescription`, `NSLocationAlwaysAndWhenInUseUsageDescription`, `NSCameraUsageDescription`, `NSPhotoLibraryUsageDescription`, `NSPhotoLibraryAddUsageDescription`, all with specific, honest copy — see Step 2 report §Task 3b/4b for why each one is present); `src/pages/auth.tsx`, `src/pages/account.tsx`, `src/pages/market.tsx` (all three call sites migrated to `src/lib/device/`, which now goes through `@capacitor/geolocation`/`@capacitor/camera` instead of raw web APIs) | Apple | **FIXED** *(iOS build/runtime unverified — no Xcode on this machine, see Step 2 report §6c)* | Step 2, Tasks 3b/4a/4b | Full-file `Info.plist` read confirming all 5 keys present with the exact copy; `npx tsc -b` + `npx oxlint src` clean; **not** verified by an actual iOS build or on-device permission prompt — that requires Xcode, which this environment doesn't have |
| LOKL-036 | Fixed *(web e2e-verified; native path unverified — no device, same standing caveat as LOKL-001)* | Chat-image sharing (`src/pages/market.tsx`, the `ChatDetailPage`'s "Share image" button) used a raw, programmatically-created `<input type="file">` rather than the `ImagePicker` abstraction — missed in Step 2's call-site list | `src/pages/market.tsx` (migrated to `ImagePicker`, Step 3 Task 5); `src/lib/device/ImagePicker.tsx` (web path changed from `<label>` to `<button>` + ref, specifically because this call site's own e2e test queries `getByRole('button', ...)` + `waitForEvent('filechooser')`, which a `<label>` wrapper doesn't satisfy) | Both | **FIXED** | Step 3, Task 5 | `npm run test:release` (mock mode) passes the full chat flow including "share image" via the real `filechooser` event path; re-audited for any other unmigrated call site (`grep` for `type="file"` / `createElement('input')` outside `src/lib/device/`) — none found, all four now migrated. Native-path camera/library picker for this call site specifically: unverified, no device. |
| LOKL-002 | Should-fix *(re-check recommended — see Step 2 note)* | No `PrivacyInfo.xcprivacy` under `ios/App/App/` (app target) | `ios/App/App/` (absent); `node_modules/@capacitor/ios/Capacitor/Capacitor/PrivacyInfo.xcprivacy` (present — Capacitor's own bundled manifest); confirmed in Step 2 that **none** of the 3 newly-installed plugins (`@capacitor/geolocation@8.2.2`, `@capacitor/camera@8.2.4`, `@capacitor/app@8.1.1`) ship their own `.xcprivacy` file either (`find node_modules/@capacitor/{geolocation,camera,app} -iname "*.xcprivacy"` → empty) | Apple | OPEN | — | **Re-adjudicated in Step 0, Task 4b; re-checked in Step 2 Task 1.** Original verdict (not currently a submission blocker, given Capacitor core's own bundled manifest and no direct required-reason API use in `AppDelegate.swift`) still holds on the facts checked so far. But now that real native plugin code is actually compiled into the binary, and NONE of it ships a manifest of its own, this needs a from-source native-code audit (do Geolocation/Camera/App's Swift implementations touch `UserDefaults`, file timestamps, or disk space APIs internally?) that requires reading their native Swift sources line-by-line or an actual Xcode build's privacy report — neither done here. Flagging as needing a real re-check once Xcode is available, rather than re-asserting the Step 0 verdict as if the new plugins changed nothing. |
| LOKL-003 | Fixed | `AndroidManifest.xml` declares only `INTERNET` | `android/app/src/main/AndroidManifest.xml` — added `ACCESS_COARSE_LOCATION` (deliberately not `ACCESS_FINE_LOCATION`: this app's feed/search is radius-based, 1-25 km presets, and never needs street-level precision) and an optional, non-required `android.hardware.location.gps` `<uses-feature>`. No camera/storage permission was needed — `@capacitor/camera`'s own bundled manifest (`node_modules/@capacitor/camera/android/src/main/AndroidManifest.xml`) declares only Android 11+ package-visibility `<queries>`, launching the system camera/gallery app via intent rather than touching hardware directly, and this app never sets `saveToGallery`. | Google Play | **FIXED** | Step 2, Task 3c/4c | `assembleDebug`/`bundleRelease` both succeeded with the plugins synced; confirmed the Camera plugin's own manifest via direct file read (no `uses-permission` in it); not confirmed by an actual runtime permission-prompt test — no device attached (see Step 2 report §6b) |
| LOKL-004 | Fixed | Zero Capacitor native plugins installed; camera, geolocation, and push are all implemented as plain web APIs | `package.json` — added `@capacitor/geolocation@8.2.2`, `@capacitor/camera@8.2.4`, `@capacitor/app@8.1.1` (all matched to the existing Capacitor 8 major; `@capacitor/app` installed per Step 2's explicit instruction for Steps 5/6, not yet used for any feature). Push (`@capacitor/push-notifications`) remains **not installed** — explicitly out of scope for Step 2, deferred to Step 6. | Both | **PARTIAL** — camera/geolocation fixed, push still open (Step 6) | Step 2, Task 1 | `npx cap sync` confirms all 3 plugins registered for both platforms; `npm ls` confirms versions |
| LOKL-005 | Critical | Firebase **web** Messaging SDK used for push; cannot deliver background/killed-state notifications inside a native WKWebView/Android WebView | `src/lib/firebase.ts:24-44`; `android/app/build.gradle:47-53` (own log message admits it); `google-services.json`/`GoogleService-Info.plist` both absent | Both | OPEN | — | Full-file read + two `find` commands |
| LOKL-006 | **Critical** *(re-rated — was Should-fix)* | **Google Sign-In is dead on native, not merely "unverified live."** `redirectTo` derives from `window.location.origin`. On Android that origin is `https://localhost` (per `androidScheme: 'https'`); on iOS it is **`capacitor://localhost`** (corrected in Step 1 — a Step 0 finding wrongly recorded this as `lokl://localhost`; `ios.scheme` never controlled the WebView's URL scheme at all, see LOKL-034). But the origin value isn't even the root cause: the OAuth consent screen is requested and rendered **inside the app's own embedded WebView** (confirmed in Step 1: zero matches anywhere in `src/` for `Browser.`, `window.open`, `target="_blank"`, or any in-app-browser plugin — there is no escape hatch). Google's OAuth 2.0 policy explicitly blocks its authorization endpoint inside embedded WebViews — Google's own documentation names both `android.webkit.WebView` and **`WKWebView` on iOS** as blocked user agents, returning `403 disallowed_useragent` (enforced since July 24, 2023). This app's Capacitor WebView is exactly that on both platforms. Compounding factor, not the root cause: no `CFBundleURLTypes` entry on iOS and no `BROWSABLE`/`VIEW` intent-filter on Android, so even a hypothetical external-browser round-trip has no registered way back into the app. **Still OPEN — out of scope for Step 1** (Step 1 was build-integrity only; fixing this needs a native browser plugin, e.g. `@capacitor/browser` with a Custom-Tabs/SFSafariViewController round-trip, which is plugin-installation work reserved for a later step). | `src/services/api.ts:198-201` (`redirectTo`, `signInWithOAuth` call — no external-browser plugin surrounding it); `package.json:20-23` (no `@capacitor/browser`); `ios/App/App/Info.plist` (no `CFBundleURLTypes`); `android/app/src/main/AndroidManifest.xml:20-23` (intent-filter present, `MAIN`/`LAUNCHER` only); `capacitor.config.ts` (`server.iosScheme: 'capacitor'`, now explicit) | Both | OPEN | — | **Investigated in Step 0 Task 3a, origin corrected in Step 1 Task 5b.** WebSearch confirmed Google's policy text and enforcement date. Not confirmed on this app's actual compiled binary on a physical device — first item on the manual test checklist. |
| LOKL-034 | Fixed | `capacitor.config.ts`'s `ios.scheme: 'Lokl'` set a nonexistent Xcode build scheme — `ios.scheme` names the scheme `cap run ios`/`cap build ios` pass to `xcodebuild -scheme`, not a URL scheme, and no shared scheme named "Lokl" (or any `.xcscheme` file at all) exists anywhere under `ios/`; the only real target/scheme is `App`. Would have made `cap run ios`/`cap build ios` fail outright looking for a scheme that doesn't exist. | `capacitor.config.ts` (was `ios: { scheme: 'Lokl' }`) | Apple (tooling, not store policy) | **FIXED** | Step 1, Task 5a | Confirmed no `.xcscheme` file exists anywhere under `ios/` (`find ios -iname "*.xcscheme"` → empty) and `project.pbxproj` has only the `App` target; changed to `ios: { scheme: 'App' }`; re-ran `npx cap sync` and confirmed the generated `ios/App/App/capacitor.config.json` reflects `"scheme": "App"`. Not confirmed by an actual `cap run ios`/`xcodebuild` invocation — no Xcode on this machine. |
| LOKL-007 | Critical | `/privacy` and `/terms` render a visible "Draft for human legal review" banner | `src/pages/legal.tsx:6-17` | Both | OPEN | — | Full-file read |
| LOKL-008 | Fixed | `src/services/api.ts` falls back to mock/localStorage mode unless `VITE_APP_MODE` is the *exact string* `supabase` — independent of whether real Supabase credentials are present | `src/lib/app-mode.ts` (new — single source of truth, throws at module load for any value other than exactly `mock`/`supabase`); `vite.config.ts` (build-time gate, throws before `dist/` is produced); `src/lib/supabase.ts` (hard-throws if `VITE_APP_MODE=supabase` but the client couldn't be constructed — no downgrade); `src/services/api.ts:20` (`useMock = __IS_MOCK_MODE__`); `src/lib/constants.ts:5` (`APP_MODE` now re-exported from `app-mode.ts`, no more `\|\| 'mock'` default) | Both | **FIXED** | Step 1, Task 1 | Executed 5 real builds: (1) `VITE_APP_MODE` unset → build fails, exit 1; (2) `VITE_APP_MODE=production` (invalid) → fails, exit 1; (3) `VITE_APP_MODE=supabase` with no credentials → fails, exit 1; (4) `VITE_APP_MODE=mock` → succeeds, exit 0; (5) `VITE_APP_MODE=supabase` with credentials present → succeeds, exit 0. All five run against the real `npm run build` command, not a simulation. |
| LOKL-009 | Fixed | `scripts/production-preflight.cjs` is not invoked by `npm run build` or any CI, and read `.env.local` only — not the files a real `vite build` actually consumes | `package.json` `scripts.build` (now `node scripts/production-preflight.cjs && tsc -b && vite build`); `scripts/production-preflight.cjs` (rewritten to read `.env`/`.env.production`/`.env.local`/`.env.production.local` in Vite's real precedence order, then overlay `process.env`, matching `loadEnv`'s documented behavior); required-var list expanded with 4 Firebase vars that were read by code but never checked — see the ledger-scope note added in Step 1 below | Both | **FIXED** | Step 1, Task 3 | `npm run build` with `VITE_APP_MODE=supabase` and only URL/key set → preflight fails before `tsc`/`vite` ever run, listing exactly the 9 missing vars + 2 gates; supplying all 14 required vars → preflight passes and the build completes. Mock-mode builds pass preflight trivially (mode-aware: skips the production-only checks). |
| LOKL-010 | **PARTIAL — build-proven, runtime-unverified** | `minifyEnabled false` in the Android release build type | `android/app/build.gradle` — now `minifyEnabled true`, `shrinkResources true`. No custom keep rules added: checked `@capacitor/geolocation`, `@capacitor/camera`, `@capacitor/app` for their own consumer ProGuard rules first — none exist; `@capacitor/android` core's own bundled `consumer-rules.pro` already keeps every `com.getcapacitor.Plugin` subclass and `@CapacitorPlugin`-annotated method, which covers all three. | Google Play | **PARTIAL** | Step 3, Task 2 | `bundleRelease`/`assembleRelease` both succeed with R8 enabled — proves R8 didn't crash at build time. **Does not** prove nothing reflective broke at runtime (Capacitor's plugin bridge is exactly the kind of reflection-heavy mechanism R8 can silently break) — that requires Task 6's on-device scenario run, which was not possible this step (no device attached). Delivered size measured via `bundletool`: post-R8 is smaller than the Step 1 pre-plugin baseline (see `docs/audit/BASELINE.md`). |
| LOKL-011 | Critical | `users` RLS `SELECT USING (true)` + `GRANT SELECT ... TO authenticated` (no `anon` grant) exposes every column, including `phone`, on every user row to any signed-in account | `supabase/schema.sql:766-768` (policy); `supabase/schema.sql:699-710` (grant); `supabase/schema.sql:21` (`phone` column) | Both | OPEN | — | **Re-confirmed in Step 0, Task 3c.** `anon` is blocked at the table-grant level (not just by policy), so unauthenticated access is not possible — but any of the app's own regular signed-in users can read every other user's phone number today. Proposed fix (not implemented — out of scope for this step): replace the blanket `select` grant/policy on `public.users` with a public, column-restricted view (e.g. `public.user_profiles_public` exposing `id, full_name, avatar_url, bio, locality, city, rating, total_reviews, is_verified, is_dealer, joined_at` and omitting `phone`), point all client reads at that view, and narrow the base table's own `select` policy to `auth.uid() = id OR current_user_is_admin()`. |
| LOKL-012 | Critical | Storage bucket `lokl-media` is `public: true`; the owner-scoped `storage.objects` SELECT policy does not actually restrict reads, because public-bucket objects are served via Supabase's unauthenticated public URL path, which never consults RLS — this includes `chat-images/{userId}`, so private chat photos are readable by anyone who obtains the URL | `supabase/schema.sql:873-874` (bucket `public=true`); `supabase/schema.sql:896` (owner-scoped policy, moot for the public path) | Both | OPEN | — | **Re-confirmed in Step 0, Task 3d.** Reasoned from Supabase's documented public-bucket serving behavior; not independently verified against a live Supabase project from this environment (no live project exists to test against) — flag as unverified-by-execution, high-confidence by documented platform behavior. |
| LOKL-013 | Should-fix | Admin self-escalation is prevented by the `protect_user_admin_fields` trigger, not by the `UPDATE` RLS policy's `WITH CHECK` itself | `supabase/schema.sql:373-388` (trigger); `supabase/schema.sql:774-778` (policy it backstops) | Both | OPEN | — | **Enriched in Step 0, Task 3e** — see report §3e for the failure-mode analysis and proposed defense-in-depth (a `WITH CHECK` clause on the policy itself asserting `is_admin` and `is_banned` are unchanged from `OLD`, in addition to the trigger, so a dropped/un-reapplied trigger during a future migration degrades to "policy still blocks it" instead of silently opening self-escalation) |
| LOKL-014 | Fixed | `npm audit` moderate finding in `@xmldom/xmldom`, via `@capacitor/cli@8.4.2 → plist@3.1.1` — build-tooling only | `package.json` `overrides: { "@xmldom/xmldom": "^0.9.12" }` — 0.9.12 is outside the vulnerable `0.9.0–0.9.11` range and satisfies `plist`'s own `^0.9.10` requirement | Neither (not shipped) | **FIXED** | Step 1, Task 6 | `npm install` then `npm audit` and `npm audit --omit=dev` both report **0 vulnerabilities**; `npm ls @xmldom/xmldom` confirms the resolved version is `0.9.12`. Chose an override over a `plist`/`@capacitor/cli` upgrade because the vulnerable dependency is two levels deep in build tooling that never ships — the smallest possible change that closes the advisory without touching `@capacitor/cli`'s own pinned version. |
| LOKL-015 | Fixed *(mechanism proven; real production keystore still doesn't exist — see LOKL-041)* | Release AAB is confirmed **unsigned** (not "likely" — proven) | `android/app/build.gradle` — `signingConfigs.release` reads credentials from `android/keystore.properties` (gitignored) or env vars, fails the build loudly if missing (`gradle.taskGraph.whenReady` check) rather than emitting an unsigned artifact | Google Play | **FIXED** | Step 3, Task 1 | Built with no credentials → `bundleRelease` fails with a clear `GradleException` before producing any artifact. Generated a throwaway, clearly-labeled verification-only keystore (CN "Lokl Verification Only, Do Not Use For Real Release", user-approved approach), built again → succeeded; `jarsigner -verify -verbose -certs` on the AAB and `apksigner verify --print-certs` on the APK both show the real certificate (not "jar is unsigned"). |
| LOKL-041 | Should-fix (blocks real submission) | **NEW.** No real production Android signing keystore exists anywhere — only the throwaway verification-only one generated in Step 3 to prove the signing mechanism works | `android/app/release-verification.keystore.jks` (gitignored, explicitly not for real use) | Google Play | OPEN | — | Confirmed no `.jks`/`.keystore` existed anywhere before Step 3 (`find` over repo + `~`); documented the real-keystore generation command and durable-storage requirement in `RELEASE.md` |
| LOKL-016 | Should-fix | Two independent service workers register on the same origin (`sw.js` app-shell cache, `firebase-messaging-sw.js` push), undocumented in README, uncoordinated | `src/main.tsx:27`; `src/lib/firebase.ts:19`; `public/sw.js`; `public/manifest.webmanifest` | Both (compounds LOKL-005) | OPEN | — | Read both registration sites and both worker source files |
| LOKL-017 | Fixed | `MOCK_ADMIN_PASSWORD = '123456'` ships as a plaintext literal in the compiled JS bundle regardless of build mode | `src/pages/account.tsx:11` (constant, now only referenced inside a build-time-eliminable branch); `src/pages/account.tsx:165` (branch now keyed on the compile-time literal `__IS_MOCK_MODE__` instead of the runtime string `api.mode`) | Neither directly (bundle hygiene / defense-in-depth) | **FIXED** | Step 1, Task 2 | Built a `supabase`-mode production bundle and grepped `dist/` for `MOCK_ADMIN_PASSWORD`, `Use 123456 in mock mode`, and `lokl-mock-db-v1` — zero hits on all three (fixed as a side effect of Task 2's broader dead-code elimination, since the entire mock-admin-unlock UI branch, not just the constant name, is now unreachable in a supabase build). |
| LOKL-018 | **PARTIAL — build-proven, on-device-unverified** | `android:allowBackup="true"` with no `dataExtractionRules`/backup exclusion, while Supabase session tokens live in WebView `localStorage` | `android/app/src/main/AndroidManifest.xml` — now `android:allowBackup="false"`. Chosen over exclusion rules: almost nothing here is worth preserving (all real data is server-side in Supabase); the main thing backup would actually carry is a live session token (a security negative) plus ephemeral local-only state (recent searches, OTP throttle counters) that costs nothing to lose. Disabling backup entirely makes both the legacy (`fullBackupContent`, pre-API-31) and modern (`dataExtractionRules`, `targetSdkVersion 36`) mechanisms moot — neither is consulted when `allowBackup` is false. | Google Play (data-safety hardening) | **PARTIAL** | Step 3, Task 4 | Confirmed via `aapt dump xmltree` on the compiled `AndroidManifest.xml` inside the built APK: `android:allowBackup(0x01010280)=(type 0x12)0x0`. **Not** verified against a real `adb backup` attempt — no device attached. |
| LOKL-019 | Should-fix | Sign in with Apple not implemented; Google OAuth is offered (and per LOKL-006 doesn't currently work on native anyway) | grep across `src/` for "sign in with apple" → no results | Apple (Guideline 4.8, borderline given phone-OTP as primary auth) | OPEN | — | Grep |
| LOKL-020 | Should-fix | `ITSAppUsesNonExemptEncryption` missing from Info.plist | `ios/App/App/Info.plist` | Apple (submission friction, not rejection) | OPEN | — | Full-file read |
| LOKL-021 | Should-fix | No unit tests anywhere; both e2e suites are hand-rolled linear scripts, not wired into any CI | `find . -iname "*.test.*" -o -iname "*.spec.*"` → zero results; no `.github/workflows`; `scripts/smoke-e2e.cjs`, `scripts/release-e2e.cjs` | Both (quality/regression risk) | OPEN | — | Confirmed via `find` |
| LOKL-022 | **Critical** *(re-rated up from the prior audit's P1 — see Task 6 note below)* | "Blocked users" writes `blocked_user_ids` but nothing reads it anywhere — feed, search, and chat never filter blocked users out | write site `src/pages/market.tsx:968` / `src/services/api.ts:563-569`; confirmed no read-site anywhere else in `src/` | Both (user safety) | OPEN | — | Grep confirms no consumer of `blocked_user_ids` outside the write path |
| LOKL-023 | Should-fix | Notifications page copy promises "price drops and trust signals," but only new-message/offer notifications are ever generated; `addNotification` exists but is never called | UI copy `src/pages/market.tsx:986`; dead call path `src/services/api.ts:496-500` | Both (docs/UI vs. code mismatch) | OPEN | — | Grep + read |
| LOKL-024 | Post-launch | Dead code: `selectedLocality`/`setSelectedLocality`, `radiusOptions`, `getRadiusAwareListingCount`, `conditionOptions[].description` | `src/store/app-store.ts:27-68`; `src/lib/constants.ts:32-39`; `src/services/local-db.ts:485-487` | Neither | OPEN | — | Grep for consumers, none found |
| LOKL-025 | Post-launch | Razorpay Edge Functions (`create-razorpay-order`, `verify-razorpay-payment`) deployed-but-orphaned, zero frontend references | `supabase/functions/create-razorpay-order/index.ts:3`; `supabase/functions/verify-razorpay-payment/index.ts:3` | Neither | OPEN | — | Grep, self-documented in code comment |
| LOKL-026 | Post-launch | Chat typing indicator never auto-clears if the user stops typing without sending | `src/services/local-db.ts:385` | Neither | OPEN | — | Read |
| LOKL-027 | Post-launch | Accessibility only spot-checked (labels/scalable text), not a full audit | across `src/pages`, `src/components` | Both (soft store-quality signal) | OPEN | — | N/A — explicitly out of scope this session |
| LOKL-028 | Post-launch | Historical media-cleanup gap self-documented for uploads predating a storage-path change | `src/pages/legal.tsx:45` | Neither | OPEN | — | Read |
| LOKL-029 | **Critical** | **Apple Guideline 4.2 (Minimum Functionality) rejection risk.** This is a bare Capacitor WebView shell with zero native plugins, no native chrome, no native transitions, and (per LOKL-001/003/005) broken permission/push integration on top of that. See Step 0 report §3b for the full assessment and ranked remediation list — this is not hedged; the blunt read is high rejection risk as currently built. | `package.json:20-23` (zero plugins); `ios/App/App/AppDelegate.swift` (unmodified boilerplate) | Apple | OPEN | — | Fetched Apple's current Guideline 4.2 text directly (`developer.apple.com/app-store/review/guidelines/`); cross-referenced against this repo's own architecture |
| LOKL-030 | Fixed *(Android: derives at build time; iOS: synced at bump time — see note)* | No single source of truth for version identifiers across `package.json` (`0.0.0`, unrelated to store versions), `android/app/build.gradle` (`versionCode 1` / `versionName "1.0"`), and iOS `pbxproj` (`CURRENT_PROJECT_VERSION = 1` / `MARKETING_VERSION = 1.0`) | New `version.json` at repo root is authoritative; `android/variables.gradle` reads it directly via `groovy.json.JsonSlurper` at Gradle-evaluation time; `android/app/build.gradle:10-11` now reads `rootProject.ext.appVersionCode`/`appVersionName` instead of hardcoded literals; `scripts/version-bump.cjs` (new) bumps `version.json`, `package.json`, and patches `project.pbxproj`'s 4 `MARKETING_VERSION`/`CURRENT_PROJECT_VERSION` occurrences in one command; `versionCode` is only ever incremented by the script, never hand-settable, so it cannot regress | Both (release-process hygiene) | **FIXED** | Step 1, Task 4 | Ran `npm run version:bump` (1.0.0/code 1 → 1.0.1/code 2), confirmed all three files updated; rebuilt the Android debug APK and read the version **out of the compiled artifact** via `aapt dump badging`: `versionCode='2' versionName='1.0.1'` — not read from source. iOS side is a text-patch-at-bump-time, not a true Xcode-build-time mechanism (adding a Run Script Build Phase to `project.pbxproj` blind, with no Xcode available here to confirm it doesn't corrupt the project file, was judged too risky to attempt unverified — flagged as a deliberate, justified deviation from the original "derives at build time" requirement for iOS specifically; Android fully meets it). |
| LOKL-035 | Should-fix (process) | `docs/audit/FINDINGS.md`'s own required-variable list previously omitted 4 Firebase vars that `src/lib/firebase.ts` actually reads — surfaced while rewriting `production-preflight.cjs` in Step 1 Task 3, cross-checked against `docs/audit/BASELINE.md`'s full `VITE_*` inventory | `scripts/production-preflight.cjs` `required` array now includes `VITE_FIREBASE_AUTH_DOMAIN`, `VITE_FIREBASE_STORAGE_BUCKET`, `VITE_FIREBASE_MESSAGING_SENDER_ID`, `VITE_FIREBASE_APP_ID` alongside the 3 that were already checked | Both | **FIXED** | Step 1, Task 3 | Cross-referenced `scripts/production-preflight.cjs`'s old `required` list against `BASELINE.md`'s full `VITE_*` usage table — no variable is checked-but-never-read; the 4 listed above were read-but-unchecked before this fix. |
| LOKL-031 | **PARTIAL — client fixed, schema deferred to Step 7** | Every new Supabase-mode user is seeded to `location_lat 12.9352 / location_lng 77.6245 / locality 'Koramangala'` (Bengaluru) by column default, and `searchListings()` falls back to those exact coordinates when no user location is supplied — silently showing another city's listings as though they were local. | Client-side fix (Step 2, Task 5): `src/services/api.ts` `upsertUserProfileFromAuth` and both new-user paths in `src/services/local-db.ts` now seed `locality: '', city: ''` instead of real place names; `src/pages/auth.tsx` `OnboardingPage` no longer pre-fills locality/city and now requires both (plus full name) before "Finish onboarding" enables; `src/pages/market.tsx` `HomePage`/`ExplorePage` now render an explicit "We don't know your neighbourhood yet" state with a "Set your location" action instead of querying with an unset locality; `searchListings()`'s `userLat`/`userLng` are no longer defaulted (now compile-time-required parameters in both `src/services/api.ts` and `src/services/local-db.ts`) — a future regression that omits them fails `tsc -b`, not silently. **Remaining, deferred to Step 7 per this step's explicit scope**: `supabase/schema.sql:25-27`'s column `DEFAULT` clauses still exist (now dead — the client never omits the field — but not removed); `location_lat`/`location_lng` remain non-nullable placeholder numbers rather than genuinely nullable (see LOKL-039). | Both | **PARTIAL** | Step 2, Task 5 | Both e2e suites updated (onboarding now fills locality/city) and passing; manually traced the full call chain from `upsertUserProfileFromAuth`/mock signup through `OnboardingPage` through `HomePage`/`ExplorePage`'s new guard. **Not** verified on a real device with a real "skip location" user flow — see Step 2 report §6b. |
| LOKL-037 | Should-fix | **NEW.** Avatar uploads (`src/pages/auth.tsx` onboarding, `src/pages/account.tsx` profile edit) are never compressed in Supabase mode — `api.uploadImage` only compresses via `fileToDataUrl` on the mock-mode path; the Supabase-mode branch uploads the raw file directly. Listing photos and chat images, by contrast, explicitly call `compressImage()` before upload. Pre-existing, not introduced by Step 2 — surfaced while auditing what `@capacitor/camera` gives "for free" vs. what the app must keep doing itself (Task 4a). | `src/services/api.ts` `uploadImage` (mock branch compresses via `fileToDataUrl`; supabase branch does not); contrast with `src/pages/market.tsx` (`compressImage(file)` called explicitly before every listing-photo and chat-image upload) | Neither (product/cost quality, not a store-policy item) | OPEN | — | Read all `uploadImage`/`compressImage` call sites; confirmed the asymmetry by inspection, not by measuring an actual uncompressed upload |
| LOKL-038 | Post-launch (Step 7) | `supabase/schema.sql`'s `location_lat`/`location_lng`/`locality`/`city` column `DEFAULT` clauses (Koramangala/Bengaluru) are now dead weight — the client never omits these fields on insert, so the defaults never fire — but they remain in the schema and could still fire for any future insert path (an Edge Function, a manual SQL insert, a future admin tool) that doesn't yet know to send empty values explicitly | `supabase/schema.sql:25-27` | Both | OPEN | — | N/A — explicitly out of scope for Step 2 per the task's own instruction; tracked for Step 7 |
| LOKL-039 | Should-fix | `UserProfile.location_lat`/`location_lng` remain typed as required, non-nullable `number` even for a user with no known location — Step 2's fix uses an empty `locality` string as the "unknown location" signal instead, leaving lat/lng as an inert Koramangala-shaped placeholder never surfaced (gated on `locality`), rather than a properly nullable pair. A full nullable-numeric migration would touch every `distanceKm`/feed/search call site and was judged out of proportion to what Step 2 asked. | `src/types.ts:22-25,52-55,158-161` (`location_lat`/`location_lng` typed `number`, not `number \| null`) | Neither (type-safety hygiene) | OPEN | — | Reviewed `types.ts`; reasoned about blast radius of a nullable migration, did not attempt it |
| LOKL-032 | **Critical** | Apple's Xcode 26 / iOS 26 SDK build requirement is confirmed **already in force** (effective April 28, 2026 — before today's date of this audit), not merely upcoming. No submission will be accepted from an older Xcode regardless of anything else in this ledger. | N/A — toolchain/process requirement, not a code location; `README.md:186` self-admits this hasn't been verified on the actual release machine | Apple | OPEN | — | **Confirmed in Step 0, Task 4c** via direct fetch of `developer.apple.com/news/upcoming-requirements/`, which states verbatim: *"Since April 28, 2026, apps uploaded to App Store Connect must be built with Xcode 26 or later using an SDK for iOS 26..."* Superseded a prior version of this audit that cited only secondary blog sources for the same claim. |
| LOKL-033 | Closed — not a defect | `lucide-react@^1.22.0` was previously flagged as a possible typosquat/version anomaly | `package.json:35` | N/A | **CLOSED** | Step 0 | **Verified in Step 0, Task 4a.** `npmjs.com/package/lucide-react` confirms the real package, current latest `1.41.0`, version `1.22.0` is a genuine point in its history (published 2026-06-28), 16,332+ dependents, canonical source `github.com/lucide-icons/lucide`. No action needed. See Step 0 report §4a for what caused the false positive and the check to run before raising a supply-chain alarm in future. |

## Note on LOKL-022's re-rating

The prior audit rated this P1. Reconsidered per the remediation owner's explicit prompt: this is a marketplace where a buyer and seller meet in person to exchange goods, chat is the pre-meetup communication channel, and "block user" is the only self-service safety control offered before that in-person meetup happens. A safety control that visibly exists in the UI (button, confirmation toast) but has zero effect is worse than not offering the feature at all, because it gives users false confidence going into an in-person transaction. Rated Critical.
